---
title: "强化学习第二篇"
slug: "rl/p2"
date: "2026-07-19"
updated: "2026-07-19"
category: "人工智能/强化学习"
tags:
  - "AI"
  - "Tutorial"
summary: "强化学习第二篇."
cover: ""
draft: false
featured: true
format: "kmd"
---

# 强化学习第二篇

## 状态价值 (State Value) 

* **定义**：状态价值函数 $v_{\pi}(s)$ 是回报 $G_t$ 在给定初始状态 $S_t = s$ 条件下的期望值。
* 公式为：$v_{\pi}(s) = \mathbb{E}[G_t\vert{}S_t = s]$。


* **核心要点**：
* 它是关于状态 $s$ 的函数，表示从该状态出发的条件期望。
* 其数值依赖于所采用的策略 $\pi$，策略不同，状态价值可能不同。
* 它衡量了状态的“价值”：较高的状态价值意味着在当前策略下能获得更高的累计奖励，代表该策略表现更好。


* **与回报 (Return) 的关系**：
* 状态价值是“从某一状态出发可能获得的所有回报”的期望值（平均值）。
* 若策略 $\pi(a\vert{}s)$、奖励概率 $p(r\vert{}s, a)$ 和状态转移概率 $p(s'\vert{}s, a)$ 均为确定性（deterministic），则状态价值等同于回报。

## 贝尔曼公式

$$v_{\pi}(s) = \mathbb{E}[R_{t+1}\vert{}S_{t}=s] + \gamma\mathbb{E}[G_{t+1}\vert{}S_{t}=s]$$

$$= \sum_{a}\pi(a\vert{}s)\sum_{r}p(r\vert{}s,a)r + \gamma\sum_{a}\pi(a\vert{}s)\sum_{s'}p(s'\vert{}s,a)v_{\pi}(s'),$$

$$= \sum_{a}\pi(a\vert{}s)\left[ \sum_{r}p(r\vert{}s,a)r + \gamma\sum_{s'}p(s'\vert{}s,a)v_{\pi}(s') \right], \quad \forall s \in \mathcal{S}.$$

%%fold {title: "证明", icon: "📂"}
Consider a random trajectory:


$$S_t \xrightarrow{A_t} R_{t+1}, S_{t+1} \xrightarrow{A_{t+1}} R_{t+2}, S_{t+2} \xrightarrow{A_{t+2}} R_{t+3}, \dots$$

The return $G_t$ can be written as


$$G_t = R_{t+1} + \gamma R_{t+2} + \gamma^2 R_{t+3} + \dots,$$

$$= R_{t+1} + \gamma(R_{t+2} + \gamma R_{t+3} + \dots),$$

$$= R_{t+1} + \gamma G_{t+1},$$

Then, it follows from the definition of the state value that


$$v_{\pi}(s) = \mathbb{E}[G_t\vert{}S_t = s]$$

$$= \mathbb{E}[R_{t+1} + \gamma G_{t+1}\vert{}S_t = s]$$

$$= \mathbb{E}[R_{t+1}\vert{}S_t = s] + \gamma\mathbb{E}[G_{t+1}\vert{}S_t = s]$$
First, calculate the first term $\mathbb{E}[R_{t+1}\vert{}S_{t} = s]$:

$$\mathbb{E}[R_{t+1}\vert{}S_{t} = s] = \sum_{a}\pi(a\vert{}s)\mathbb{E}[R_{t+1}\vert{}S_{t}=s, A_{t}=a]$$

$$= \sum_{a}\pi(a\vert{}s)\sum_{r}p(r\vert{}s,a)r$$

Second, calculate the second term $\mathbb{E}[G_{t+1}\vert{}S_{t} = s]$:

$$\mathbb{E}[G_{t+1}\vert{}S_{t} = s] = \sum_{s'} \mathbb{E}[G_{t+1}\vert{}S_{t} = s, S_{t+1} = s']p(s'\vert{}s)$$

$$= \sum_{s'} \mathbb{E}[G_{t+1}\vert{}S_{t+1} = s']p(s'\vert{}s)$$

$$= \sum_{s'} v_{\pi}(s')p(s'\vert{}s)$$

$$= \sum_{s'} v_{\pi}(s') \sum_{a} p(s'\vert{}s, a)\pi(a\vert{}s)$$
%%

## 矩阵向量形式贝尔曼公式

回想：


$$v_{\pi}(s) = \sum_{a}\pi(a\vert{}s)\left[ \sum_{r}p(r\vert{}s,a)r + \gamma\sum_{s'}p(s'\vert{}s,a)v_{\pi}(s') \right]$$

将贝尔曼方程重写为


$$v_{\pi}(s) = r_{\pi}(s) + \gamma\sum_{s'}p_{\pi}(s'\vert{}s)v_{\pi}(s') \quad (1)$$

其中


$$r_{\pi}(s) \triangleq \sum_{a}\pi(a\vert{}s)\sum_{r}p(r\vert{}s,a)r, \quad p_{\pi}(s'\vert{}s) \triangleq \sum_{a}\pi(a\vert{}s)p(s'\vert{}s,a)$$

对于状态索引为 $s_i$ ($i=1, \dots, n$) 的情况，贝尔曼方程为：


$$v_{\pi}(s_i) = r_{\pi}(s_i) + \gamma \sum_{s_j} p_{\pi}(s_j\vert{}s_i)v_{\pi}(s_j)$$

将所有状态的这些方程汇总，可以改写为矩阵-向量形式：


$$v_{\pi} = r_{\pi} + \gamma P_{\pi} v_{\pi}$$

其中：

* $v_{\pi} = [v_{\pi}(s_1), \dots, v_{\pi}(s_n)]^T \in \mathbb{R}^n$
* $r_{\pi} = [r_{\pi}(s_1), \dots, r_{\pi}(s_n)]^T \in \mathbb{R}^n$
* $P_{\pi} \in \mathbb{R}^{n \times n}$ 是状态转移矩阵，其中 $[P_{\pi}]_{ij} = p_{\pi}(s_j\vert{}s_i)$

## 贝尔曼方程求解

贝尔曼方程的矩阵向量形式为：


$$v_{\pi} = r_{\pi} + \gamma P_{\pi} v_{\pi}$$

* 闭式解为：

$$v_{\pi} = (I - \gamma P_{\pi})^{-1} r_{\pi}$$



在实际应用中，我们仍然需要使用数值工具来计算矩阵的逆。
可以通过迭代算法避免求逆。
* 迭代解为：

$$v_{k+1} = r_{\pi} + \gamma P_{\pi} v_{k}$$



该算法导出一个序列 $\{v_{0}, v_{1}, v_{2}, \dots\}$。我们可以证明：

$$v_{k} \to v_{\pi} = (I - \gamma P_{\pi})^{-1} r_{\pi}, \quad k \to \infty$$
%%fold {title: "证明", icon: "📂"}
证明。
定义误差为 $\delta_k = v_k - v_{\pi}$。我们只需要证明 $\delta_k \to 0$。将 $v_{k+1} = \delta_{k+1} + v_{\pi}$ 和 $v_k = \delta_k + v_{\pi}$ 代入 $v_{k+1} = r_{\pi} + \gamma P_{\pi} v_k$ 得到


$$\delta_{k+1} + v_{\pi} = r_{\pi} + \gamma P_{\pi}(\delta_k + v_{\pi}),$$


这可以重写为


$$\delta_{k+1} = -v_{\pi} + r_{\pi} + \gamma P_{\pi} \delta_k + \gamma P_{\pi} v_{\pi} = \gamma P_{\pi} \delta_k.$$


因此，


$$\delta_{k+1} = \gamma P_{\pi} \delta_k = \gamma^2 P_{\pi}^2 \delta_{k-1} = \cdots = \gamma^{k+1} P_{\pi}^{k+1} \delta_0.$$


注意 $0 \le P_{\pi}^k \le 1$，这意味着对于任何 $k=0, 1, 2, \dots$，$P_{\pi}^k$ 的每个元素都不大于 $1$。这是因为 $P_{\pi}^k \mathbf{1} = \mathbf{1}$，其中 $\mathbf{1} = [1, \dots, 1]^T$。另一方面，由于 $\gamma < 1$，我们知道 $\gamma^k \to 0$，因此当 $k \to \infty$ 时，$\delta_{k+1} = \gamma^{k+1} P_{\pi}^{k+1} \delta_0 \to 0$
%%

## 动作价值函数

智能体从某一状态做某一动作所得到的平均回报。衡量哪个动作更好

定义：


$$q_{\pi}(s, a) = \mathbb{E}[G_t\vert{}S_t = s, A_t = a]$$

* $q_{\pi}(s, a)$ 是关于状态-动作对 $(s, a)$ 的函数。
* $q_{\pi}(s, a)$ 依赖于 $\pi$。

根据条件期望的性质，有


$$\underbrace{\mathbb{E}[G_t\vert{}S_t = s]}_{v_{\pi}(s)} = \sum_{a} \underbrace{\mathbb{E}[G_t\vert{}S_t = s, A_t = a]}_{q_{\pi}(s, a)} \pi(a\vert{}s)$$

因此，


$$v_{\pi}(s) = \sum_{a} \pi(a\vert{}s)q_{\pi}(s, a)$$

回想一下，状态价值由下式给出：


$$v_{\pi}(s) = \sum_{a}\pi(a\vert{}s)\underbrace{\left[ \sum_{r}p(r\vert{}s,a)r + \gamma\sum_{s'}p(s'\vert{}s,a)v_{\pi}(s') \right]}_{q_{\pi}(s,a)}$$

于是动作价值函数为：


$$q_{\pi}(s, a) = \sum_{r}p(r\vert{}s,a)r + \gamma\sum_{s'}p(s'\vert{}s,a)v_{\pi}(s')$$

## 贝尔曼最优公式

### 最优策略
如果对于所有的 $s$ 以及任何其他策略 $\pi$，都满足 $v_{\pi^*}(s) \ge v_{\pi}(s)$，则称策略 $\pi^*$ 为最优策略。

### 贝尔曼最优方程：


$$v(s) = \max_{\pi} \sum_{a} \pi(a\vert{}s) \left( \sum_{r} p(r\vert{}s, a)r + \gamma \sum_{s'} p(s'\vert{}s, a)v(s') \right), \quad \forall s \in \mathcal{S}$$

$$= \max_{\pi} \sum_{a} \pi(a\vert{}s)q(s, a), \quad s \in \mathcal{S}$$

### 贝尔曼最优方程（矩阵向量形式）：


$$v = \max_{\pi} (r_{\pi} + \gamma P_{\pi} v)$$

### 最优求解
首先固定 $v(s')$ 并求解 $\pi$：


$$v(s) = \max_{\pi} \sum_{a} \pi(a\vert{}s) \left( \sum_{r} p(r\vert{}s, a)r + \gamma \sum_{s'} p(s'\vert{}s, a)v(s') \right), \quad \forall s \in \mathcal{S}$$

$$= \max_{\pi} \sum_{a} \pi(a\vert{}s)q(s, a)$$

受上述例子启发，考虑到 $\sum_{a} \pi(a\vert{}s) = 1$，我们有


$$\max_{\pi} \sum_{a} \pi(a\vert{}s)q(s, a) = \max_{a \in \mathcal{A}(s)} q(s, a),$$

其中最优性在以下情况达成


$$\pi(a\vert{}s) = \begin{cases} 1 & a = a^* \\ 0 & a \neq a^* \end{cases}$$


其中 $a^* = \arg \max_a q(s, a)$。